# Server port (bound dynamically to Render's PORT environment variable)
server.port=${PORT:8080}

# Application Name
spring.application.name=Bajaj-API-Test

# Disable excessive spring logging for cleaner test output
logging.level.org.springframework=INFO
logging.level.com.bajaj.apitest=DEBUG
